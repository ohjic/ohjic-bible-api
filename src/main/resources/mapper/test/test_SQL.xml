<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">

    <!-- list 구현 sql -->
    <select id="testSql" resultType="ohjic.bible.ohbibleVo.MainVO" >
        SELECT * from article
    </select>

    <select id="testSqlForParam" resultType="ohjic.bible.ohbibleVo.MainVO" parameterType="int">
        SELECT * from article limit #{limitCount}
    </select>

</mapper>